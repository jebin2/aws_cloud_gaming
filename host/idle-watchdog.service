[Unit]
Description=Shut down the host when no stream traffic is flowing
After=tailscaled.service

[Service]
Type=oneshot
# Written by install-watchdog.sh only when notifications are configured.
EnvironmentFile=-/etc/cg-notify.conf
ExecStart=/usr/local/bin/idle-watchdog.sh
